version: '3.8'

services:
  postgres_certification_Exam: 
    container_name: postgres_certification_Exam
    image: postgres
    ports:
      - 5434:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=pg_certification


